import { css } from "@/root/styled-system/css";

export const IconButtonClasses = {
  iconCalendar: css({
    pos: "relative",
    width: "300px",
  }),
  Icon: css({
    display: "block",
    height: "1.5rem",
    pointerEvents: "none",
    position: "absolute",
    right: "1.25rem",
    top: "50%",
    transform: "translateY(-50%)",
    width: "1.5rem",
  }),
};
